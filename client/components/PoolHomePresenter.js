import React from 'react'
import styled from 'styled-components'

const PoolHomePresenter = ({ pool, bets, user }) => {
  return (
    <div> Hello World </div>
  )
}

export default PoolHomePresenter
